export class Serie{

    constructor(public serieId: number, public nombre: string, public canal: string, public temporadas: number, public descripcion: string,
        public URLSerie: string,public URLImagen: string){
    }
}